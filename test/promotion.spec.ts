import * as mongoose from "mongoose";
import * as chai from "chai";
import * as supertest from "supertest";

import { PromotionModel } from "../src/models/ModelGenerator";

module.exports = (request: supertest.SuperTest<supertest.Test>, model: any) => {
    describe("", () => {
        before(async () => {

        });
    });

};